name: Build And Deploy

on:
    workflow_dispatch:

jobs:
    Build:
        runs-on: ubuntu-latest

        steps:
            - name: Get the code
              uses: actions/checkout@v7
            - name: Step ?
              run: ls -l -a
            # build

